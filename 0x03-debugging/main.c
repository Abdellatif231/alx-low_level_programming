#include <stdio.h>
#include "main.h"
/**
  * main - Entry point of the program
  * positive_or_negative - print if number neg or pos
  *
  * Return: Always 0 (Success)
  */

int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
