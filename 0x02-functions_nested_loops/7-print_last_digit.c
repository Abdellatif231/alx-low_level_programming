#include "main.h"
/**
 * print_last_digit - prints the last digit of a num
 * @i: the number
 *
 * Return: the last digit of i
 */

int print_last_digit(int i)
{
	int l;

	l = i % 10;
	_putchar("l");
	return (l);
}
